# Multi Step Form — product catalogue

A three-step product form inside a modal dialog, alongside a product list whose pagination lives
in the URL.

## Getting started

```bash
pnpm install
pnpm dev
```

The app starts on [http://localhost:3000](http://localhost:3000). Requires Node 20+ and pnpm 10+.
